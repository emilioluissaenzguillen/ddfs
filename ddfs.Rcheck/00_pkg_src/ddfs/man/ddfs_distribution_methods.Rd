% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{d.ddfs}
\alias{d.ddfs}
\alias{p.ddfs}
\alias{q.ddfs}
\alias{r.ddfs}
\title{Density, Distribution, Quantile, and Random Sampling Methods for ddfs Objects}
\usage{
d.ddfs(object, x)

p.ddfs(object, q)

q.ddfs(object, p)

r.ddfs(object, N)
}
\arguments{
\item{object}{A \code{ddfs} object, typically returned by \code{\link{ddfs}}.}

\item{x, q}{Points at which to evaluate the density or distribution function.}

\item{p}{Probabilities at which to evaluate the quantile function.}

\item{N}{Number of random values to generate.}
}
\value{
A numeric vector:
\itemize{
  \item \code{d.ddfs}: estimated density values at the specified points.
  \item \code{p.ddfs}: estimated cumulative distribution values at the
  specified quantiles.
  \item \code{q.ddfs}: estimated quantiles corresponding to the specified
  probabilities.
  \item \code{r.ddfs}: \code{N} random samples drawn from the estimated
  distribution.
}
}
\description{
These functions provide access to the estimated model from a fitted
\code{ddfs} object. They allow evaluating the estimated probability density
function (\code{d.ddfs}), cumulative distribution function (\code{p.ddfs}),
quantile function (\code{q.ddfs}), and generate random samples (\code{r.ddfs}).
}
\details{
For fits above two dimensions, supply a numeric matrix or data frame
with one observation per row and one column per fitted dimension. Named
columns must match the fitted dimension names and are reordered automatically;
unnamed columns use the training order. Missing values are not accepted.
Density is zero outside the fitted rectangle. CDF coordinates are clamped to
their fitted ranges, including infinite coordinates, so upper-bound coordinates
evaluate the corresponding marginal CDF. Quantiles and random sampling are
available only for univariate fits.
}
\examples{
set.seed(123)

# Simulate data
N <- 1000
sim <- sim.dist(N = N, ex = "GEV Type III")
X <- sim$X

# Fit a DDFS model
GmodDens <- ddfs(X, n = 4, min.intknots = 3, phi_F = 0.3, q_F = 1, beta = 0.1)

# Evaluate density, cdf, and quantiles on new data
X_new <- sim.dist(N = 10, ex = "GEV Type III")$X
d.ddfs(GmodDens, X_new)
p.ddfs(GmodDens, X_new)
q.ddfs(GmodDens, p = c(0.25, 0.5, 0.75))

# Generate random samples
r.ddfs(GmodDens, N = 10)

}
\seealso{
\code{\link{ddfs}}
}
