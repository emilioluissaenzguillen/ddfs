% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{TVaR}
\alias{TVaR}
\alias{TVaR.ddfs}
\title{Tail Value at Risk (TVaR) for \code{ddfs} objects}
\usage{
TVaR(object, ...)

\method{TVaR}{ddfs}(object, alpha, tail = "right", ...)
}
\arguments{
\item{object}{An object of class \code{ddfs}, typically returned from
\code{\link{ddfs}}.}

\item{...}{Potentially further arguments (required by the definition of the
generic function). These will be ignored, but with a warning.}

\item{alpha}{Numeric value in (0,1) specifying the tail probability. For example,
if \code{alpha = 0.99} and \code{tail = "right"}, TVaR estimates the expected
loss given that the loss exceeds the 99th percentile.}

\item{tail}{A character string, either \code{"right"} or \code{"left"}, indicating
whether to compute the upper or lower tail expectation.}
}
\value{
A single \code{numeric} value giving the estimated Tail Value at Risk
(i.e., conditional expectation in the tail above/below the \eqn{\alpha}-quantile).
}
\description{
Computes the \emph{Tail Value at Risk} (TVaR), at a given confidence level for
a fitted \code{ddfs} object.
}
\details{
The right-tail TVaR at level \code{alpha} is defined as the expected loss
conditional on exceeding the \code{alpha}-quantile. The left-tail TVaR corresponds
to the expected value below the \code{alpha}-quantile.
}
\seealso{
\code{\link{VaR}}
}
